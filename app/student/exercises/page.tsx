import { Card } from '@/components/ui/Card'

export default function ExercisesPage() {
  return (
    <main className="p-6">
      <h1 className="mb-4 text-xl font-semibold">Esercizi di analisi delle strutture</h1>
      <Card>
        {/*
          TODO: 4 tipi de ejercicio (esercizio_struttura_1..4).
          Contrato exacto de cada tipo pendiente de definir — ver
          lib/gemini/schema.ts > valutazioneStrutturaSchema (placeholder genérico)
          y lib/gemini/prompts/struttura.ts (pendiente de crear).
        */}
      </Card>
    </main>
  )
}
